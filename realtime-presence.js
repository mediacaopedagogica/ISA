import { createClient } from 'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm'
import { CONFIG } from './config.js'
const dbP=createClient(CONFIG.SUPABASE_URL,CONFIG.SUPABASE_KEY,{auth:{persistSession:true,autoRefreshToken:true}})
let meP=null,familyP=[],channelP=null,onlineSet=new Set(),lastSeen={}
const $p=id=>document.getElementById(id)
async function identity(){const {data:{user}}=await dbP.auth.getUser();if(!user)return null;const {data:m}=await dbP.from('family_members').select('id,family_id,display_name').eq('auth_user_id',user.id).eq('active',true).single();if(!m)return null;meP=m;const {data:f}=await dbP.from('family_members').select('id,display_name').eq('family_id',m.family_id).eq('active',true);familyP=f||[];const {data:ps}=await dbP.from('presence_state').select('member_id,last_seen_at').in('member_id',familyP.map(x=>x.id));lastSeen=Object.fromEntries((ps||[]).map(x=>[x.member_id,x.last_seen_at]));return m}
function fmtLast(ts){if(!ts)return'Offline';const d=new Date(ts),now=new Date();const same=d.toDateString()===now.toDateString();return `Visto por último ${same?'hoje às '+d.toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'}):d.toLocaleDateString('pt-BR')+' às '+d.toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}`}
function repaint(){for(const card of document.querySelectorAll('.family-card')){const name=card.querySelector('h3')?.textContent?.trim(),m=familyP.find(x=>x.display_name===name);if(!m)continue;const s=card.querySelector('small');if(s){const on=onlineSet.has(m.id);s.innerHTML=`<i class="presence-dot ${on?'online':''}"></i>${on?'Online':fmtLast(lastSeen[m.id])}`}}
const onlineCount=$p('onlineCount');if(onlineCount)onlineCount.textContent=familyP.filter(x=>onlineSet.has(x.id)).length
const title=$p('chatTitle')?.textContent?.trim(),m=familyP.find(x=>x.display_name===title);if(m&&$p('chatSubtitle'))$p('chatSubtitle').textContent=onlineSet.has(m.id)?'Online':fmtLast(lastSeen[m.id])}
async function touch(online){if(!meP)return;await dbP.from('presence_state').upsert({member_id:meP.id,online,last_seen_at:new Date().toISOString(),device_label:/Mobile/i.test(navigator.userAgent)?'Celular':'Computador'},{onConflict:'member_id'}).catch(()=>{})}
async function start(){if(channelP)return;const m=await identity();if(!m)return;await dbP.realtime.setAuth().catch(()=>{});channelP=dbP.channel(`family:${m.family_id}`,{config:{private:true,presence:{key:m.id}}});channelP.on('presence',{event:'sync'},()=>{const state=channelP.presenceState();onlineSet=new Set(Object.keys(state));repaint()});channelP.on('presence',{event:'join'},({key})=>{onlineSet.add(key);repaint()});channelP.on('presence',{event:'leave'},({key})=>{onlineSet.delete(key);lastSeen[key]=new Date().toISOString();repaint()});channelP.subscribe(async status=>{if(status==='SUBSCRIBED'){await channelP.track({memberId:m.id,at:new Date().toISOString()});await touch(true);repaint()}});setInterval(()=>{if(document.visibilityState==='visible')touch(true)},30000);document.addEventListener('visibilitychange',async()=>{if(document.visibilityState==='visible'){await channelP.track({memberId:m.id,at:new Date().toISOString()});await touch(true)}else{await channelP.untrack();await touch(false)}repaint()});window.addEventListener('pagehide',()=>{channelP?.untrack();touch(false)})}
const obs=new MutationObserver(()=>repaint());obs.observe(document.documentElement,{childList:true,subtree:true});dbP.auth.onAuthStateChange((_e,s)=>{if(s)start();else{if(channelP)dbP.removeChannel(channelP);channelP=null;meP=null;onlineSet.clear()}});start()
