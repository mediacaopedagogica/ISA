/* Nossa Rede — publicação, Story e Capa com editor de foto depois da escolha do arquivo. */
import { createClient } from 'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm'
import { CONFIG } from './config.js?v=20260910-media-workflow'

(function(){
  if(window.__ISA_NOSSA_REDE_MEDIA_WORKFLOW__)return
  const db=createClient(CONFIG.SUPABASE_URL,CONFIG.SUPABASE_KEY,{auth:{persistSession:true,autoRefreshToken:true}})
  const $=id=>document.getElementById(id),q=(s,r=document)=>r?.querySelector?.(s)||null,qa=(s,r=document)=>[...(r?.querySelectorAll?.(s)||[])]
  const token=new URLSearchParams(location.hash.replace(/^#/,'')).get('acesso')||''
  const FILTERS={
    natural:['Natural','none'],brilho:['Brilho','brightness(1.13) saturate(1.08)'],doce:['Doce','brightness(1.08) saturate(1.12) contrast(.96)'],rose:['Rosé','sepia(.12) saturate(1.18) hue-rotate(320deg) brightness(1.04)'],ceu:['Céu','saturate(1.12) hue-rotate(178deg) brightness(1.03)'],verao:['Verão','saturate(1.24) sepia(.08) brightness(1.05)'],filme:['Filme','contrast(1.12) saturate(.88) brightness(.97)'],vintage:['Vintage','sepia(.26) contrast(.94) saturate(.82) brightness(1.04)'],quente:['Quente','sepia(.2) saturate(1.16) brightness(1.04)'],frio:['Frio','saturate(.92) hue-rotate(176deg) brightness(1.04)'],pb:['P&B','grayscale(1) contrast(1.06)'],dramatico:['Dramático','contrast(1.22) saturate(1.18) brightness(.92)'],suave:['Suave','brightness(1.08) contrast(.88) saturate(.9)']
  }
  let own=null,editorState=null,urls=[]
  const external=()=>!!$('friendApp')&&!!token
  const root=()=>$('familySocialOverlay')||$('socialPanel')
  const mediaInput=()=>external()?$('fsMedia'):$('socialMediaInput')
  const toast=text=>{const t=$('friendToast')||$('toast');if(!t)return;t.textContent=text;t.classList.remove('hidden');clearTimeout(t._mw);t._mw=setTimeout(()=>t.classList.add('hidden'),2800)}
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))
  function css(){if($('isaMediaWorkflowCss'))return;const l=document.createElement('link');l.id='isaMediaWorkflowCss';l.rel='stylesheet';l.href='./nossa-rede-media-workflow-v1.css?v=1-edit-delete-story-cover';document.head.appendChild(l)}
  async function identity(){if(own)return own;if(external()){const p=window.__ISA_FRIEND_PERSON__;if(p?.id)return own={id:p.id,family_id:p.family_id||'',name:p.name||'Família'};return null}const {data:{user}}=await db.auth.getUser();if(!user)return null;const {data}=await db.from('family_members').select('id,family_id,display_name').eq('auth_user_id',user.id).eq('active',true).maybeSingle();own=data;return own}
  async function externalUpload(action,file){const form=new FormData();form.append('token',token);form.append('action',action);form.append('file',file);const r=await fetch(`${CONFIG.SUPABASE_URL}/functions/v1/friend-social`,{method:'POST',headers:{apikey:CONFIG.SUPABASE_KEY},body:form,cache:'no-store'});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Não foi possível enviar a imagem.');return d}
  async function uploadStory(file){if(file.size>50*1024*1024)return toast('O story pode ter até 50 MB.');try{toast('Publicando story…');if(external())await externalUpload('upload_story',file);else{const who=await identity();if(!who)throw new Error('Perfil não encontrado.');const ext=(file.name.split('.').pop()||(file.type.startsWith('video/')?'mp4':'jpg')).replace(/[^a-z0-9]/gi,'').toLowerCase(),id=crypto.randomUUID(),path=`${who.family_id}/${who.id}/stories/${id}.${ext}`;const up=await db.storage.from('social-media').upload(path,file,{contentType:file.type||undefined,upsert:false});if(up.error)throw up.error;const ins=await db.from('social_stories').insert({id,family_id:who.family_id,author_id:who.id,media_type:file.type.startsWith('video/')?'video':'image',storage_path:path,mime_type:file.type,expires_at:new Date(Date.now()+86400000).toISOString()});if(ins.error){await db.storage.from('social-media').remove([path]);throw ins.error}}toast('Story publicado por 24 horas 💕');window.__ISA_FAMILY_SOCIAL_EXTRAS__?.scan?.(true)}catch(e){toast(e.message||'Não foi possível publicar o story.')}}
  async function uploadCover(file){if(file.size>8*1024*1024)return toast('A capa pode ter até 8 MB.');try{toast('Atualizando a capa…');if(external())await externalUpload('upload_cover',file);else{const who=await identity();if(!who)throw new Error('Perfil não encontrado.');const ext=(file.name.split('.').pop()||'jpg').replace(/[^a-z0-9]/gi,'').toLowerCase(),path=`${who.family_id}/${who.id}/cover/${crypto.randomUUID()}.${ext}`,old=(await db.from('social_profiles').select('cover_ref').eq('member_id',who.id).maybeSingle()).data?.cover_ref||'';const up=await db.storage.from('social-media').upload(path,file,{contentType:file.type,upsert:false});if(up.error)throw up.error;const u=await db.from('social_profiles').update({cover_ref:path,updated_at:new Date().toISOString()}).eq('member_id',who.id);if(u.error)throw u.error;if(old)await db.storage.from('social-media').remove([old]).catch(()=>{})}toast('Capa atualizada ✨');window.__ISA_FAMILY_SOCIAL_EXTRAS__?.scan?.(true)}catch(e){toast(e.message||'Não foi possível atualizar a capa.')}}
  function picker(accept,cb){const i=document.createElement('input');i.type='file';i.accept=accept;i.style.display='none';document.body.appendChild(i);i.onchange=()=>{const f=i.files?.[0];i.remove();if(f)cb(f)};i.click()}
  function pickStory(){picker('image/jpeg,image/png,image/webp,image/gif,video/mp4,video/webm,video/quicktime',f=>{if(f.type.startsWith('image/')&&!/gif/i.test(f.type))openEditor(f,{title:'Editar imagem do Story',applyText:'Publicar Story',onApply:uploadStory});else uploadStory(f)})}
  function pickCover(){picker('image/jpeg,image/png,image/webp',f=>openEditor(f,{title:'Editar capa',applyText:'Usar como capa',onApply:uploadCover}))}
  function clearUrls(){urls.forEach(u=>{try{URL.revokeObjectURL(u)}catch{}});urls=[]}
  function selectedFiles(input=mediaInput()){return window.__ISA_NUVEM_CAROUSEL__?.getFiles?.(input)||[...(input?.files||[])]}
  function setFiles(input,files){if(window.__ISA_NUVEM_CAROUSEL__?.setFiles)return window.__ISA_NUVEM_CAROUSEL__.setFiles(input,files);try{const dt=new DataTransfer();files.forEach(f=>dt.items.add(f));input.files=dt.files;input.dispatchEvent(new Event('change',{bubbles:true}))}catch{}return files}
  function renderTray(){const r=root(),input=mediaInput();if(!r||!input)return;clearUrls();let tray=q('.isa-media-workflow-tray',r);if(!tray){tray=document.createElement('div');tray.className='isa-media-workflow-tray';const preview=q('#socialPreview,#fsPreview',r);(preview||input).insertAdjacentElement('afterend',tray)}const files=selectedFiles(input);tray.style.display=files.length?'flex':'none';tray.innerHTML=files.map((f,i)=>{const u=URL.createObjectURL(f);urls.push(u);const media=f.type.startsWith('video/')?`<video src="${u}" muted playsinline></video>`:`<img src="${u}" alt="Prévia">`;return `<article class="isa-media-item" data-media-index="${i}"><div class="isa-media-thumb">${media}</div><small class="isa-media-name">${esc(f.name)}</small><div class="isa-media-actions">${f.type.startsWith('image/')&&!/gif/i.test(f.type)?'<button type="button" class="isa-media-edit" data-media-edit>✨ Editar</button>':''}<button type="button" class="isa-media-delete" data-media-delete>✕ Excluir</button></div></article>`}).join('')}
  function bindTray(){const r=root();if(!r||r.dataset.isaMediaTrayBound==='1')return;r.dataset.isaMediaTrayBound='1';r.addEventListener('click',e=>{const card=e.target.closest?.('[data-media-index]');if(!card)return;const index=Number(card.dataset.mediaIndex),input=mediaInput(),files=selectedFiles(input);if(e.target.closest('[data-media-delete]')){e.preventDefault();files.splice(index,1);setFiles(input,files);setTimeout(renderTray,20);toast('Imagem removida antes de publicar.')}else if(e.target.closest('[data-media-edit]')){e.preventDefault();const f=files[index];if(!f)return;openEditor(f,{title:'Editar foto da publicação',applyText:'Aplicar efeito',onApply:edited=>{const now=selectedFiles(input);if(index<now.length){now[index]=edited;setFiles(input,now);setTimeout(renderTray,30);toast('Efeito aplicado à foto ✨')}}})}},true)}
  function editor(){let o=$('isaMediaEditor');if(o)return o;o=document.createElement('section');o.id='isaMediaEditor';o.innerHTML=`<div class="isa-media-editor-card"><div class="isa-media-editor-head"><div class="grow"><h3 id="isaMediaEditorTitle">Editar foto</h3><small>Escolha um efeito. Você pode voltar ao original antes de aplicar.</small></div><button class="isa-media-editor-close" type="button">✕</button></div><div class="isa-media-editor-preview"><img id="isaMediaEditorImage" alt="Prévia da edição"></div><div id="isaMediaFilters" class="isa-media-filters">${Object.entries(FILTERS).map(([k,v])=>`<button type="button" class="isa-media-filter ${k==='natural'?'active':''}" data-media-filter="${k}">${v[0]}</button>`).join('')}</div><div class="isa-media-editor-actions"><button type="button" class="original" data-media-original>Original</button><button type="button" class="apply" data-media-apply>Aplicar</button></div></div>`;document.body.appendChild(o);q('.isa-media-editor-close',o).onclick=()=>closeEditor();q('[data-media-original]',o).onclick=()=>selectFilter('natural');q('[data-media-apply]',o).onclick=applyEditor;o.addEventListener('click',e=>{const b=e.target.closest('[data-media-filter]');if(b)selectFilter(b.dataset.mediaFilter);else if(e.target===o)closeEditor()});return o}
  function openEditor(file,opts={}){if(!file?.type?.startsWith('image/'))return opts.onApply?.(file);if(/gif/i.test(file.type))return toast('GIF animado é mantido original.');const o=editor(),url=URL.createObjectURL(file);editorState={file,filter:'natural',url,opts};$('isaMediaEditorTitle').textContent=opts.title||'Editar foto';$('isaMediaEditorImage').src=url;$('[data-media-apply]')?.replaceChildren(document.createTextNode(opts.applyText||'Aplicar'));selectFilter('natural');o.classList.add('show')}
  function closeEditor(){const o=$('isaMediaEditor');o?.classList.remove('show');if(editorState?.url)try{URL.revokeObjectURL(editorState.url)}catch{}editorState=null}
  function selectFilter(k){if(!editorState||!FILTERS[k])return;editorState.filter=k;qa('[data-media-filter]',editor()).forEach(b=>b.classList.toggle('active',b.dataset.mediaFilter===k));$('isaMediaEditorImage').style.filter=FILTERS[k][1]}
  async function processImage(file,filter){if(filter==='natural')return file;const img=new Image(),url=URL.createObjectURL(file);try{await new Promise((res,rej)=>{img.onload=res;img.onerror=rej;img.src=url});const max=2400,scale=Math.min(1,max/Math.max(img.naturalWidth,img.naturalHeight)),w=Math.max(1,Math.round(img.naturalWidth*scale)),h=Math.max(1,Math.round(img.naturalHeight*scale)),c=document.createElement('canvas');c.width=w;c.height=h;const ctx=c.getContext('2d');ctx.filter=FILTERS[filter]?.[1]||'none';ctx.drawImage(img,0,0,w,h);const type=file.type==='image/png'?'image/png':'image/jpeg',blob=await new Promise(r=>c.toBlob(r,type,type==='image/jpeg'?.92:undefined));if(!blob)throw new Error('Não foi possível aplicar o efeito.');const base=file.name.replace(/\.[^.]+$/,'')||'foto',ext=type==='image/png'?'png':'jpg';return new File([blob],`${base}-${filter}.${ext}`,{type,lastModified:Date.now()})}finally{URL.revokeObjectURL(url)}}
  async function applyEditor(){if(!editorState)return;const s=editorState,btn=q('[data-media-apply]',editor());btn.disabled=true;btn.textContent='Aplicando…';try{const out=await processImage(s.file,s.filter);closeEditor();await s.opts.onApply?.(out)}catch(e){toast(e.message||'Não foi possível editar a foto.')}finally{btn.disabled=false;btn.textContent=s.opts.applyText||'Aplicar'}}
  function patchMenu(){const r=root();if(!r)return;const menu=q('.nuvem-compose-menu',r);if(!menu)return;const upload=q('[data-nuvem-compose="upload"]',menu);if(upload){upload.querySelector('.nuvem-menu-copy b')?.replaceChildren(document.createTextNode('Publicação'));upload.querySelector('.nuvem-menu-copy small')?.replaceChildren(document.createTextNode('Escolher foto ou vídeo'))}if(!q('[data-isa-media-destination="story"]',menu)){const b=document.createElement('button');b.type='button';b.className='nuvem-compose-item';b.dataset.isaMediaDestination='story';b.innerHTML='<span class="nuvem-icon-soft"><span>⭕</span></span><span class="nuvem-menu-copy"><b>Story • 24h</b><small>Upload de foto ou vídeo</small></span>';b.onclick=e=>{e.preventDefault();e.stopPropagation();menu.classList.remove('show');pickStory()};upload?.insertAdjacentElement('afterend',b)}if(!q('[data-isa-media-destination="cover"]',menu)){const b=document.createElement('button');b.type='button';b.className='nuvem-compose-item';b.dataset.isaMediaDestination='cover';b.innerHTML='<span class="nuvem-icon-soft"><span>🌷</span></span><span class="nuvem-menu-copy"><b>Capa do perfil</b><small>Upload e edição da capa</small></span>';b.onclick=e=>{e.preventDefault();e.stopPropagation();menu.classList.remove('show');pickCover()};q('[data-isa-media-destination="story"]',menu)?.insertAdjacentElement('afterend',b)}}
  function bindInput(){const input=mediaInput();if(!input||input.dataset.isaMediaWorkflowBound==='1')return;input.dataset.isaMediaWorkflowBound='1';input.addEventListener('change',()=>setTimeout(renderTray,35));document.addEventListener('isa:social-media-selection-changed',()=>setTimeout(renderTray,20))}
  function removeStandaloneEffects(){qa('.isa-effects-btn').forEach(x=>x.remove())}
  function scan(){css();patchMenu();bindInput();bindTray();removeStandaloneEffects();renderTray()}
  new MutationObserver(()=>setTimeout(scan,60)).observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('isa:friend-portal-entered',scan);document.addEventListener('click',e=>{if(e.target.closest?.('#socialNav,#friendSocialBtn,[data-social-open]'))setTimeout(scan,180)},true);setTimeout(scan,300);[900,1800,3200].forEach(ms=>setTimeout(scan,ms));window.__ISA_NOSSA_REDE_MEDIA_WORKFLOW__={scan,pickStory,pickCover,openEditor,uploadStory,uploadCover,renderTray}
})();
